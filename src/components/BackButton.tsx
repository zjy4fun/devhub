import React from 'react';
import {MutedText} from './MutedText.js';

/**
 * Small helper text for back navigation.
 */
export function BackButton() {
  return <MutedText>`Esc` / `q` Back</MutedText>;
}
